[package]
name = "tela-utils"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "tela 通用工具库：Version 三元组、JSON 处理、时间处理（零平台依赖）"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
