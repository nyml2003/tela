[package]
name = "tela-log"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "tela 内部日志库（零依赖，输出适配器可注入：web console / stderr / 测试收集）"

[dependencies]

[lints]
workspace = true
