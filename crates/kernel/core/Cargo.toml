[package]
name = "tela-core"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "tela 核心：树构建与校验、布局、绘制命令、命中测试、焦点转移、模态栈、视图状态仓库（渲染器无关，纯 Rust）"

[dependencies]
tela-contract = { path = "../contract" }

[lints]
workspace = true
