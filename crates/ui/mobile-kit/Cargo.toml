[package]
name = "tela-mobile-ui-kit"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Tela mobile UI kit: safe-area, touch-target, list and action semantics"

[dependencies]
tela-contract = { path = "../../kernel/contract" }
tela-core = { path = "../../kernel/core" }
tela-ui-headless = { path = "../headless" }
tela-ui-foundation = { path = "../foundation" }

[lints]
workspace = true
