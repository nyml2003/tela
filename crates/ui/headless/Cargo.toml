[package]
name = "tela-ui-headless"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Tela headless UI semantics: signals, component parts, and typed event routing"

[dependencies]
tela-contract = { path = "../../kernel/contract" }
tela-core = { path = "../../kernel/core" }

[lints]
workspace = true
