# 016-已解决问题归档

> 本文只记录已经有源码、测试或交付门禁证据的问题。它们不再占用“已知限制/未来扩展”清单；后续回归时只需补充证据或重新打开对应条目。

## 1. 核心能力

| 原噪音问题 | 当前结论 | 证据 |
|---|---|---|
| `auto-stable-identity` 仍是算法草案，无法保证重排后状态保持 | ✅ 已实现：按内容指纹匹配、类型变化换身份、延迟回收 | [`identity.rs`](../crates/tela-core/src/identity.rs)、[`m4_identity_state.rs`](../crates/tela-core/tests/m4_identity_state.rs) |
| 视图状态会随树重建丢失 | ✅ `ViewStateStore` 按稳定 key 复用并回收焦点、滚动、选中和模态状态 | [`state.rs`](../crates/tela-core/src/state.rs)、[`m4_identity_state.rs`](../crates/tela-core/tests/m4_identity_state.rs) |
| Dirty 更新只能停留在设计层，无法验证局部重算 | ✅ `LayoutCache` 与 `resolve_dirty` 已实现，Full/Dirty 有回归测试 | [`update.rs`](../crates/tela-core/src/update.rs)、[`m5_update.rs`](../crates/tela-core/tests/m5_update.rs) |
| 焦点、模态拦截、Teleport、快捷键和 Save/Restore 没有闭环 | ✅ M6 交互原语已落地并覆盖合成事件测试 | [`m6_interact.rs`](../crates/tela-core/tests/m6_interact.rs) |

## 2. 宿主与演示

| 原噪音问题 | 当前结论 | 证据 |
|---|---|---|
| canvas 不是完整客户端视口，页面会按浏览器文档流向下滚动 | ✅ 浏览器宿主以 CSS viewport 驱动逻辑 viewport，并同步 DPR/backing store | [`014-文件管理器演示与组件运行时.md`](014-文件管理器演示与组件运行时.md)、[`web/src/main.ts`](../web/src/main.ts) |
| CPU、WGPU、自动回退三种路径可能各自维护一套 UI | ✅ 三种后端共享同一 `UiNode -> UiFrame`，差异只在最终绘制 | [`014-文件管理器演示与组件运行时.md`](014-文件管理器演示与组件运行时.md)、`ops build all --gpu`、`ops verify demo` |
| 文件管理器代码堆在单个页面文件，组件边界不清 | ✅ 已按 application/domain/host/presentation 拆分，页面区域组件独立维护 | [`crates/tela-demo/src`](../crates/tela-demo/src)、[`014-文件管理器演示与组件运行时.md`](014-文件管理器演示与组件运行时.md) |
| 左侧目录树有 hover，但点击后经常不切换目录 | ✅ 修正目录行的有限命中宽度，补充真实 core 命中路径和文件选择回归测试 | [`navigation.rs`](../crates/tela-demo/src/presentation/navigation.rs)、[`runtime.rs`](../crates/tela-demo/src/application/runtime.rs) |
| 组合组件与原子组件混在 `tela-widgets`，工具栏只能直接发业务 BindId | ✅ `tela-ui` 成为组合组件归属，`Toolbar` 通过 `UiIntent::Invoke` 交由 demo Controller 路由 | [`crates/tela-ui`](../crates/tela-ui)、[`017-tela-ui第一阶段实施目标.md`](017-tela-ui第一阶段实施目标.md) |
| 文件列表以“30 行 × 固定高度”猜测滚动上限，长目录切换到短目录会留下旧偏移，视觉上像顶部被 clip | ✅ `UiFrame.scroll_bounds` 输出真实边界，滚动状态进入 `ViewStateStore`；`VirtualWindow` 只构建可见项，目录与内容集切换统一回到顶部 | [`resolve.rs`](../crates/tela-core/src/resolve.rs)、[`virtual_window.rs`](../crates/tela-ui/src/virtual_window.rs)、[`runtime.rs`](../crates/tela-demo/src/application/runtime.rs) |
| 文件行的 Material 图标看似被列表 clip，`hero.png` 图片图标顶部缺笔画 | ✅ 根因是文字 renderer 将字形墨迹裁进自身布局盒；`GlyphInkBounds` 让 Raster/WGPU 都保留真实墨迹范围，仅按祖先 clip 裁剪 | [`glyphs.rs`](../crates/tela-text/src/glyphs.rs)、[`text.rs`](../crates/tela-render-wgpu/src/text.rs)、[`snapshot.rs`](../crates/tela-render-raster/tests/snapshot.rs) |

## 3. 文档噪音

- “M6 交互仍待定义”已过时；M6 的剩余范围只保留在 [011-已知限制与未来扩展清单](011-已知限制与未来扩展清单.md) 中。
- `005-key身份策略` 中的身份算法草案已改为实现说明；未实现的开发期策略误用告警仍单独保留。
- `014-文件管理器演示与组件运行时` 不再重复维护完整后置能力列表，演示范围与核心限制分别以本文、[011](011-已知限制与未来扩展清单.md) 和 [015](015-tela-ui与局部交互状态.md) 为准。

## 4. 归档规则

新增问题只有在源码、测试或交付门禁可复核后才能移入本文。实现尚未完成、只有设计或需要宿主接入的能力，继续放在 011 或对应设计文档，避免把目标描述误当成现状。
