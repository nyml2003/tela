[package]
name = "tela-cc-protocol"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "CC Remote shared protocol: relay events, phone REST and agent wire messages, net bridge payloads, TCP framing"

[dependencies]
postcard = { version = "1", features = ["alloc"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
