[package]
name = "tela-cc-agent"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "CC Remote desktop agent: drives the local claude CLI and bridges it to the relay"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tela-cc-protocol = { path = "../cc-protocol" }

[lints]
workspace = true
