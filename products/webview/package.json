{
  "name": "tela-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test:wire": "esbuild bridge-wire.test.mjs --bundle --format=esm --outfile=dist-test/bridge-wire.test.mjs && node dist-test/bridge-wire.test.mjs"
  },
  "dependencies": {
    "solid-js": "^1.9.14"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "esbuild-plugin-solid": "^0.6.0",
    "typescript": "^7.0.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}